// Keep the fail-closed native-canary contract inside the repository's default
// src/**/*.test.ts discovery surface while the coordinator remains tool-owned.
import '../../../../../tools/llm-wiki-cli/src/headless/native-canary/coordinator.failclosed.test';
